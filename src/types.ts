export interface TokenInfo {
  mint: string;
  creator: string;
  initialPrice: number;
  name: string;
  symbol: string;
  devBuy: number;
  devAmount: number;
  mintBlock: number;
  migrated: boolean;
}